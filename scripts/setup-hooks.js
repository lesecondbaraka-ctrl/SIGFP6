#!/usr/bin/env node
// Minimal setup-hooks stub to avoid script failure.
// You can extend this to install Git hooks (e.g., with Husky) if needed.

console.log('[setup-hooks] No hooks configured. This is a stub script.');
